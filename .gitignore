# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Environment / secrets
.env
.env.*
!.env.example

# Accidental empty clone folder
hazard-waste-detection/

# IDE / OS
.idea/
.vscode/
.cursor/
*.swp
Thumbs.db
.DS_Store

# Stray Ultralytics validation outputs (not part of documented runs)
runs/segment/

# Datasets — images and labels stay local only (not published to GitHub)
hazard_dataset/images/
hazard_dataset/labels/
hazard_dataset_clean/images/
hazard_dataset_clean/labels/
hazard_dataset_clean/labels/**/*.cache
hazard_dataset_clean/labels/**/*.cache.npy

# Demo / QA / report figures (may contain scrap-yard photos)
demo/*.jpg
demo/*.png
demo/*.jpeg
demo/*.webp
visual_qa/
evaluation_reports/*.png
evaluation_reports/*.jpg

# Training / evaluation plot images (keep JSON/TXT reports)
runs/**/evaluation/**/*.png
runs/**/evaluation/**/*.jpg
runs/**/train/**/*.jpg
runs/**/train/**/*.png

# Nested vendor git metadata
yolov9/.git/

# YOLOv9 training artifacts (production weight tracked via LFS)
yolov9/runs/train-seg/**/*
!yolov9/runs/train-seg/hazard_waste_seg/
!yolov9/runs/train-seg/hazard_waste_seg/weights/
!yolov9/runs/train-seg/hazard_waste_seg/weights/best.pt
yolov9/runs/val-seg/
yolov9/runs/predict-seg/

# Ultralytics duplicate train weights (canonical copies in runs/*/weights/)
runs/yolo11s_run2/train/
runs/yolov8s_run3/train/
runs/yolov5s_run4/train/
# Legacy YOLOv9 training visuals (may contain dataset photos)
yolov9/runs/
yolov9/data/images/
yolov9/figure/
