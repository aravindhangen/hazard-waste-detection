services:
  hazard-detection:
    build: .
    image: hazard-waste-detection:latest
    container_name: hazard-waste-detection
    ports:
      - "8000:8000"
    environment:
      KMP_DUPLICATE_LIB_OK: "TRUE"
      HAZARD_DEVICE: cpu
      PORT: "8000"
    restart: unless-stopped
