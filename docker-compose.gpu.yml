# GPU override — use with: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up --build
services:
  hazard-detection:
    environment:
      HAZARD_DEVICE: "0"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
